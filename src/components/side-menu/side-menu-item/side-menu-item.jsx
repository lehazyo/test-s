import React from "react";
import styles from './side-menu-item.module.css';

export const SideMenuItem = ({ icon, title, selected }) => {
  return (
    <div className={styles.wrapper}>
      
    </div>
  );
};
